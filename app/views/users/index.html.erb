<div class="pure-layout pure-layout-960">

	<div class="text-center">
		<h1>All Users</h1>
	</div>

	<ul class="pure-list-striped gc-user-list">
	<% @users.each do |user| %>
		<li><%= link_to user.nickname, user %></li>
	<% end %>
	</ul>
	<%= will_paginate @users %>
</div>